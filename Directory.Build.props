<Project TreatAsLocalProperty="Platform">
  <PropertyGroup>
    <!-- Diligent 不支持 AnyCPU，自动重定向为 x64 -->
    <Platform
      Condition="'$(Platform)' == '' Or '$(Platform)' == 'AnyCPU' Or '$(Platform)' == 'Any CPU'">x64</Platform>
  </PropertyGroup>
</Project>